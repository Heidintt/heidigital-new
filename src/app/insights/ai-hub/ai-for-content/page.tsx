import type { Metadata } from "next"; import Link from "next/link"; import { ChevronRight, ArrowRight } from "lucide-react"; import ArticleCard from "@/components/cards/ArticleCard"; import { articles } from "@/data/articles";
export const metadata: Metadata = { title: "AI for Content Creation | Heidigital", description: "Best practices for using AI tools in content creation — from blog writing and social media to email and ad copy." };
export default function AIForContentPage() {
  const contentAIArticles = articles.filter((a) => a.categorySlug === "ai-hub/ai-for-content" || (a.tags?.includes("AI") && a.tags?.includes("Content"))).slice(0, 6);
  const displayArticles = contentAIArticles.length > 0 ? contentAIArticles : articles.slice(0, 3);
  return (<><section className="pt-28 pb-16 bg-background-secondary"><div className="container-max"><nav className="flex items-center gap-2 text-sm text-text-muted mb-6"><Link href="/" className="hover:text-text-main">Home</Link><ChevronRight className="w-3.5 h-3.5" /><Link href="/insights/ai-hub" className="hover:text-text-main">AI Hub</Link><ChevronRight className="w-3.5 h-3.5" /><span className="text-text-main">AI for Content</span></nav><span className="badge-primary mb-4 inline-flex">AI Hub</span><h1 className="text-4xl md:text-5xl font-bold font-display text-text-main mb-4">AI for Content Creation</h1><p className="text-lg text-text-muted max-w-2xl">Using AI tools for blog posts, social media, email campaigns, and ad copy — without losing your brand voice or quality.</p></div></section><section className="section-padding"><div className="container-max"><div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">{displayArticles.map((a) => <ArticleCard key={a.slug} article={a} />)}</div><div className="mt-12 text-center"><Link href="/resources/templates/ai-prompt-pack" className="btn-primary">Get AI Prompt Pack <ArrowRight className="w-4 h-4" /></Link></div></div></section></>);
}
