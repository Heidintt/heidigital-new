import type { Metadata } from "next"; import Link from "next/link"; import { ChevronRight, ArrowRight } from "lucide-react"; import CourseCard from "@/components/cards/CourseCard"; import { courses } from "@/data/courses";
export const metadata: Metadata = { title: "AI Marketing Courses | Heidigital", description: "Learn how to use AI tools for digital marketing — ChatGPT, Gemini, Canva AI, and AI marketing workflows." };
export default function AIMarketingCoursesPage() {
  const aiCourses = courses.filter((c) => c.categorySlug === "ai-marketing");
  const fallback = courses.slice(0, 3);
  const displayCourses = aiCourses.length > 0 ? aiCourses : fallback;
  return (<><section className="pt-28 pb-16 bg-background-secondary"><div className="container-max"><nav className="flex items-center gap-2 text-sm text-text-muted mb-6"><Link href="/" className="hover:text-text-main">Home</Link><ChevronRight className="w-3.5 h-3.5" /><Link href="/resources/courses" className="hover:text-text-main">Courses</Link><ChevronRight className="w-3.5 h-3.5" /><span className="text-text-main">AI Marketing</span></nav><span className="badge-primary mb-4 inline-flex">AI Marketing</span><h1 className="text-4xl md:text-5xl font-bold font-display text-text-main mb-4">AI Marketing Courses</h1><p className="text-lg text-text-muted max-w-2xl">Learn how to use AI tools like ChatGPT, Gemini, and Canva AI to supercharge your marketing workflows.</p></div></section><section className="section-padding"><div className="container-max"><div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">{displayCourses.map((c) => <CourseCard key={c.id} course={c} />)}</div><div className="mt-12 text-center"><Link href="/insights/ai-hub" className="btn-primary">AI Hub <ArrowRight className="w-4 h-4" /></Link></div></div></section></>);
}
